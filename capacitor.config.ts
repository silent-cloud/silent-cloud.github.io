import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.silentcloudapp',
  appName: 'sielnt-cloud-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
